import styled from "@emotion/styled";
import font from "@env/Fonts";

export const RestrictAccessContainer = styled.div`
  text-align: center;
  padding: 48vh;

  h1 {
    color: #fff;
    font-family: ${font.logo};
    margin: 1rem;
    font-size: 4vh;
  }
`;
